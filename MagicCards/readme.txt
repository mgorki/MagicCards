Install the dependencies if necessary. 
Make sure the arduino is conected to serial port COM3 or COM4. 
For starting the main program run run_experiment.py in the src folder or directly run experiment_main in the experiment subfolder.
